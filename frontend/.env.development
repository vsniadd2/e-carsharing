# Прямое подключение SignalR к API (обязательно после пересборки контейнера/проекта с хабами).
# Если negotiate даёт 404 — выполните: docker compose build api && docker compose up -d api
VITE_PUBLIC_API_ORIGIN=http://127.0.0.1:8080
